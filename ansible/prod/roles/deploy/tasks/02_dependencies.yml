- name: Install dependencies with uv sync
  command:
    cmd: uv sync --group prod
    chdir: "{{ app_dir }}"
  environment:
    UV_PYTHON: "python3.13"
  notify:
    - restart app
    - restart background_main
